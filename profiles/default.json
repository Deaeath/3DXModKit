{
  "description": "Adaptive memory management. Releases Unity assets in-process (when the runtime plugin is installed), then trims the working set - but only while the game is backgrounded, minimised, or idle. System-wide purges only under real pressure.",
  "enabled": [
    "runtime-bridge",
    "3dxgc"
  ],
  "config": {
    "runtime-bridge": {
      "unloadOnBackground": true,
      "unloadOnIdle": true,
      "minSecondsBetweenUnloads": 120
    },
    "3dxgc": {
      "triggers": {
        "onBlur":     { "enabled": true, "delaySeconds": 20 },
        "onMinimize": { "enabled": true, "delaySeconds": 3 },
        "onIdle":     { "enabled": true, "idleSeconds": 180 }
      },
      "systemWide": {
        "enabled": true,
        "onlyWhenCritical": true
      }
    }
  }
}
